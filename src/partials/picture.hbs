<picture {{#if id}} id="{{ id }}" {{/if}}>
    <source srcset="{{ file }}.webp" type="image/webp">
    <source srcset="{{ file }}.{{ ext }}" type="image/{{ type }}">
    <img title="{{ title }}" src="{{ file }}.{{ ext }}" loading="{{ loading }}" alt="{{ alt }}">
</picture>