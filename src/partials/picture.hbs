<picture>
    <source srcset="{{ filename }}.webp" type="image/webp">
    <source srcset="{{ filename }}.{{ ext }}" type="image/{{ type }}">
    <img title="{{ title }}" src="{{ filename }}.{{ ext }}">
</picture>